# tools/__init__.py
# Package initializer for the tools module.
# Exposes all LangChain-compatible tool definitions used by the agents.
# Tools are implemented as @tool-decorated functions or BaseTool subclasses
# so they can be bound directly to LangGraph nodes via agent executors.
